#!/bin/bash
git update-index --assume-unchanged .project
git update-index --assume-unchanged .classpath
git update-index --assume-unchanged .settings/*
git update-index --assume-unchanged .externalToolBuilders/*

